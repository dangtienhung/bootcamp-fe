/*
1. get -> dùng để lấy dữ liệu từ server
2. post -> dùng để gửi dữ liệu lên server
3. put -> dùng để cập nhật dữ liệu lên server
{email && password}
4. patch -> dùng để cập nhật dữ liệu lên server
{ password }
5. delete -> dùng để xóa dữ liệu trên server
*/
